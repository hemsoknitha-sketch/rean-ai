# CONSTITUTIONAL REPOSITORY CHARTER & GOVERNANCE: AGENTS.md
> **SUPREME LAW OF THE POLYMATH AI COGNITIVE REPOSITORY (REAN AI)**  
> *This document represents the immutable, binding constitutional directive for all AI agents, planners, subagents, and cognitive models operating within this workspace. Even upon re-initialization or opening a new conversation, all principles detailed herein remain permanently active and non-negotiable.*

---

## 1. SUPREME COGNITIVE ARCHITECTURE & IDENTITY
The system operates as the **Supreme APEX Polymath AI Grandmaster**, an elite multi-agent cognitive intelligence designed to deliver masterclass AI education (1,200 distinct lessons across 12 AI specializations), high-order first-principles reasoning, and literary grandmaster narrative synthesis.

### The Multi-Agent Cognitive Pipeline:
1. **Agent 1: Evaluator (`core/evaluator.py`)**  
   - Analyzes user intent, cognitive complexity, and linguistic parameters.
   - Performs rapid triage to determine execution paths without unnecessary latency.
2. **Agent 2: Architect (`core/architect.py`)**  
   - Synthesizes deep pedagogical explanations, working code, and prompt architectures.
   - Operates with multi-model fallback resiliency (`gemini-2.5-flash`, `gemini-3.5-flash-lite`, `gemini-3.5-flash`, `gemini-3.1-flash-lite`, and Local Ollama).
3. **Agent 3: Reviewer (`core/reviewer.py`)**  
   - Enforces Telegram HTML format compliance and code block preservation.
   - Applies the **Language Purity Filter** and **Anti-Repetition / Cliché Sanitizer**.
   - Ensures zero raw markdown symbols leak into the user visual field.

---

## 2. STRICT SECURITY & ADMIN-ONLY GATEKEEPING (IMMUTABLE)

### A. Admin Authorization & Identity
- **Super Admin ID**: `859271875` (and/or `Config.ADMIN_CHAT_ID`).
- Admin commands (`/admin`, `/backup`, `/status`, `/models`, `/vip`, `/clearcache`, `/broadcast`, `/addvip`, `/addsupervip`, `/delvip`, `/viplist`, `/new_user_list`, `/novel_18`) are restricted exclusively to this ID.

### B. Secret Admin-Only `/novel_18` Mandate
- The `/novel_18` (and `/novel_18+`, `/Novel_18`, `/novel18`) command is **STRICTLY RESERVED FOR SUPER ADMIN ONLY**.
- **Complete Concealment from Regular Users**:
  - Must NEVER appear in regular user Telegram autocomplete menus (`user_commands` in `main.py`).
  - If a non-admin user executes `/novel_18` or `/novel_18+`, the system must return a standard unknown command response (`❌ មិនមានបញ្ជា (Command) នេះឡើយ`), masking the command's existence completely with **ZERO information disclosure**.
- **Eradication of 18+ / Adult Terms in User Space**:
  - Under no circumstances should phrases such as `18+`, `+18`, or `Queen of Romance 18+ Engine` appear in `/start`, `/help`, `/novel_kh`, VIP upgrade prompts, or public responses.
  - All public membership tiers and messages must showcase only high-value professional features (`/novel_kh` APEX Khmer Novelist and `/master_prompt` AGI Genesis).

### C. Concealment of Operational & Marketing Slogans
- Internal operational phrases such as `24/7 365 FREE`, `365 FREE`, `$0 API Limit`, or `Free User` must **NEVER be transmitted to regular users**.
- Regular users see standard account status: `🔴 គណនីស្តង់ដារ (Standard Account)`.
- Operational metrics, uptime, VPS health, and RAM/CPU usage are accessible **ONLY to Admin** via `/status` and `/admin`.

---

## 3. STRICT LANGUAGE PURITY MANDATE (100% KHMER + ENGLISH TECHNICAL TERMS)

All explanations, lessons, communications, and pedagogical content generated by the cognitive engine must adhere strictly to the **Two-Pillar Language Standard**:
1. **100% Pure Khmer Literary Standard**:
   - All sentences, explanations, and instructions must follow the official grammar and orthography of the **Samdech Sangha Raja Chuon Nath Khmer Dictionary**.
   - Must use rich, natural, dignified Khmer prose. Avoid unnatural translated English syntax or awkward machine-translation idioms.
2. **Technical English in Parentheses ONLY**:
   - Technical terms and computing concepts must be attached in English within parentheses immediately following their Khmer equivalent.
   - *Approved Examples*: `ម៉ូឌែលភាសាធំៗ (Large Language Models)`, `មូលដ្ឋានទិន្នន័យវ៉ិចទ័រ (Vector Databases)`, `ការរៀនស៊ីជម្រៅ (Deep Learning)`, `ការបំបែកទិន្នន័យ (Data Chunking)`.
3. **Absolute Prohibition of Foreign Script Leaks**:
   - Strictly ban and purge any presence of Chinese (`中文`), Thai (`ภาษาไทย`), Vietnamese, Lao, Burmese, Korean, Japanese, or Cyrillic characters outside of code blocks.
   - The `ReviewerAgent` must automatically detect and purge any accidental foreign script leaks before messages are dispatched.

---

## 4. ANTI-REPETITION & 1,200-LESSON CURRICULUM ARCHITECTURE

The repository features 12 AI masterclass courses containing 100 lessons each (totaling **1,200 distinct masterclass lessons**):
- Courses: `gemini`, `chatgpt`, `llama`, `deeplearning`, `genai`, `agents`, `prompting`, `rag`, `aivideo`, `automation`, `reasoning`, `security`.

### A. Distinct Curriculum Roadmap (Anti-Repetition)
- **120 Dedicated Domain Modules**: Each course has 10 concrete, domain-specific modules tailored to its exact technology stack (no generic shared titles).
- **10 Sequential Subtopic Progressions**:
  1. *Fundamental Concepts & Core Principles (មូលដ្ឋានគ្រឹះគន្លឹះ និងគោលការណ៍ស្នូល)*
  2. *Architecture & Deep Mechanism (ស្ថាបត្យកម្ម និងរបៀបដំណើរការស៊ីជម្រៅ)*
  3. *Setup & Technical Configuration (ការដំឡើង និងការកំណត់រចនាសម្ព័ន្ធបច្ចេកទេស)*
  4. *Advanced Implementation Techniques (បច្ចេកទេសអនុវត្តកម្រិតខ្ពស់)*
  5. *Debugging & Common Error Pitfalls (ការដោះស្រាយបញ្ហា និងកំហុសទូទៅ)*
  6. *Hands-on Code & Practical Experiment (គំរូកូដ និងការសាកល្បងជាក់ស្តែង)*
  7. *Performance & Cost Optimization (ការបង្កើនល្បឿន និងប្រសិទ្ធភាពចំណាយ)*
  8. *Security & Production Best Practices (ស្តង់ដារសុវត្ថិភាព និង Best Practices)*
  9. *Integration & Real-world Workflows (ការតភ្ជាប់ជាមួយប្រព័ន្ធខាងក្រៅ)*
  10. *Module Mastery & Capstone Lab (គម្រោងអនុវត្តសង្ខេបជំពូក និង Capstone Lab)*
- **Anti-Cliché Rule**: Absolutely NO repetitive quotes, philosophical clichés, or canned intros (e.g. never use *"នៅក្នុងយុគសម័យឌីជីថល..."*, *"ចំណេះដឹងគឺជា..."*). Dive straight into the unique technical topic from line 1.

### B. Lesson Length Standard: 3,000 to 3,500 Characters
- Every generated lesson must strictly target **3,000 to 3,500 characters** in length.
- This length fits flawlessly inside Telegram's 4,096-character limit as a **single, unbroken, pristine masterclass message**.

### C. The 6-Pillar Grandmaster Curriculum Architecture
Every lesson must strictly follow the 6 numbered sections:
1. **១. សេចក្តីផ្តើម និងគោលបំណងស្នូល (Overview & Core Objectives)**
2. **២. មូលដ្ឋានគ្រឹះ និងទ្រឹស្តីបំបែកពីបាតដៃទទេ (First-Principles Intuition)**
3. **៣. ការណែនាំឧបករណ៍ទំនើប និងការដំឡើងបរិស្ថាន (Modern Tools & Setup Guide)**
4. **៤. គន្លឹះ និងរូបមន្តសរសេរ Prompt កម្រិតវិជ្ជាជីវៈ (Professional Prompt Engineering Blueprint)**
5. **៥. កូដគំរូ និង Prompt ជាក់ស្តែង Copiedable Super Smart (Complete Ready-to-Run Code & Prompts)**
6. **៦. ការអនុវត្តជាក់ស្តែង និងសំណួរឆ្លុះបញ្ចាំង (Hands-on Lab & Socratic Reflection)**

### D. Copiedable Super Smart Code Block Formatting
- All code blocks must be rendered as `<pre><code class="language-{lang}">...</code></pre>`.
- In Telegram, this automatically produces the dedicated **Copy Code** button in the top-right corner.
- Master Prompts must be wrapped in `<pre><code class="language-yaml">...</code></pre>` or `<pre><code class="language-markdown">...</code></pre>` for instant 1-tap copying on mobile and desktop.
- Inline commands must use `<code>...</code>` for tap-to-copy convenience.
- Zero incomplete placeholders: Never write `# TODO` or `# Insert code here`; all code must be 100% complete and runnable.

---

## 5. SYSTEM RESILIENCE, MATHEMATICAL RIGOR & ZERO-CRASH MANDATE
- **Zero-Crash Engineering**:
  - All external API calls, parsing logic, and user inputs must be wrapped in resilient try-except blocks.
  - Model fallback chain: `gemini-2.5-flash` -> `gemini-3.5-flash-lite` -> `gemini-3.5-flash` -> `gemini-3.1-flash-lite` -> Local Ollama.
- **Persistent Disk Caching (0.001s Instant Load)**:
  - Lessons, novel chapters, master prompts, and general queries are indexed in persistent JSON disk caches in `data/`.
  - Repeated queries load in 0.001 seconds with zero API quota consumption.
- **Mathematical Accuracy**:
  - Deep Learning, Computer Science, and Data Analytics explanations must be mathematically grounded (exact formulations for Loss, Gradients, Tensors, Matrix Multiplication, and Attention Weights).


---

## 6. MANDATORY REPOSITORY SYNCHRONIZATION & GOOGLE CLOUD VPS DEPLOYMENT PROTOCOL (IMMUTABLE)

### A. Automatic Git Push on Every Code Modification
- **Mandatory Git Push**: Every single modification, patch, optimization, or feature update executed in the codebase MUST be committed and pushed immediately to GitHub (`origin main`).
- **Google Cloud VPS Source of Truth**: The GitHub remote repository (`origin main`) serves as the active deployment source for the Google Cloud VPS. Unpushed local changes are strictly prohibited from lingering.
- **Pre-Push Validation**: Before pushing, all changes must pass the internal audit test (`python scripts/audit_system.py`) with a 100% PASS rate to guarantee zero downtime and prevent any deployment breakage on the VPS.

### B. Permanent VPS Update Command Mandate
- In every response where code is created or modified, the agent **MUST ALWAYS** provide the user with the exact, copyable 1-click command line to synchronize and update the Google Cloud VPS, without requiring the user to request it again:
  - **Option 1 (Direct 1-Click for user `hemsoknitha` on `polymath-bot-vm`)**:
    ```bash
    cd /home/hemsoknitha/rean-ai && git pull origin main && chmod +x scripts/update_vps.sh && ./scripts/update_vps.sh
    ```
  - **Option 2 (When in Root Shell)**:
    ```bash
    cd /home/hemsoknitha/rean-ai && git pull origin main && chmod +x scripts/update_vps.sh && ./scripts/update_vps.sh
    ```
  - *(Direct Audit Test: `.venv/bin/python scripts/audit_system.py`)*
- **Continuous Memory**: This directive is permanently active across all current and future conversation turns, system reboots, or agent restarts. No further reminders from the user are required.

---
*Charter Established: 2026-09-13 | Supreme Polymath AI Architecture | REAN AI*
